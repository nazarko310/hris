import './Admin.css'
import {Fragment} from "react";

export default function Admin({users, calendarDate}) {
    console.log(users, calendarDate)
    return (
        <Fragment>
            {

            }
        </Fragment>
    )
}
